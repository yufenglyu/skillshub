# 开发与修复任务

已实现项目如下；尚待人工验收和后续讨论的事项单独列出。发布记录见中英文 CHANGELOG。

## 已实现

- [x] 标签：重命名、删除及关联同步；Ctrl 多选交集筛选；标题行清除筛选，折叠时隐藏按钮。
- [x] 拖拽标签：普通拖拽为选中技能补充目标标签，Shift 拖拽仅移除目标标签；高亮目标并消除文字选区。目录不提供标签。
- [x] 备注：技能与资源库目录备注可搜索；技能 AI 生成可编辑建议，保存后写入，清空立即删除对应内容。
- [x] 搜索：范围包含仓库名称、技能名称、描述、备注，不含标签；筛选按钮内嵌；Ctrl+F 定位，Esc 清空并退出。
- [x] 导入：仅 GitHub 和本地文件夹入口；GitHub 使用“导入”；简化向导顶部，提供取消操作，后台执行后刷新列表。
- [x] 导入冲突：按实际目标目录判断覆盖；不同目录的同 ID 技能独立导入，重新导入保留目标记录 ID。
- [x] 工具栏：纯图标；包裹加号表示添加，下载箭头表示更新，循环箭头表示刷新，保留悬停提示。
- [x] 更新技能：仓库默认折叠；全部、新增、待更新、远程删除、无变化、检查失败、已忽略筛选；窗口可缩放且筛选不改变高度。
- [x] 重查：底部提供重查所选、重试失败项、检查全部；重查所选保留其他仓库结果，失败重试优先处理已选失败仓库。
- [x] 应用：按技能选择；检查并发 5、应用并发 2，同仓库检查与写入互斥；成功项转为无变化，失败项保留。
- [x] 内容校验：仅文件内容变化触发更新；校验版本，精确匹配已成功新增或更新的重复提交直接成功；区分分类变化和远程内容变化。
- [x] 新旧替换：同仓库替换候选归入远程删除，一对一自动配对、多候选手动选择；默认不选中，提供“删除并重新导入”，保留原 ID、元数据及关联。
- [x] 忽略与任务：忽略仅针对当前版本；保留最近 100 条任务记录，重启后未完成任务标为中断，由用户重试。
- [x] 平台：右键添加、编辑、启用/停用、删除；未检测到目录则停用，侧栏隐藏停用项，空安装数显示 0。
- [x] 平台图标：配置保存相对路径，图标位于配置目录 icons/，支持自定义与恢复默认，默认图标随包提供。
- [x] 布局：侧栏平台/项目数量，随页面与选择变化的状态栏统计，搜索靠右，备注按钮间距及细表头线。

## 待验收与后续讨论

- [ ] 在实际桌面环境验收拖拽、缩放、高 DPI 布局、AI 服务和退出时的后台任务提示。
- [ ] 持续核查不规范仓库的更新；现有回归通过不代表覆盖全部仓库布局。
- [ ] 按仓库定制更新规则：分支、包含/排除目录、旧新路径映射及仅更新已有技能；目前仅为建议，尚未实现。

共享中心扩展功能已取消，不在本轮范围。
