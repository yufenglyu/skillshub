# 更新日志

本文件记录该项目的重要变更。

## 0.80.0 - 2026-08-21

本版本取消龙虾类 / 编程类拆分，统一为软件平台；平台配置改为 `platform.json` + `icons/`；中文「中央技能库」更名为「技能中心」。

### 新功能

- 侧栏与设置页取消龙虾类、编程类，统一为**软件平台**。
- 设置页可为每个软件平台启用或关闭（与项目目录相同）；状态写入 `platform.json` 的 `enabled` 字段。
- 平台配置改为 `.skillshub/platform/platform.json`，图标放在 `.skillshub/platform/icons/`（启动时会迁移旧的「每平台一个 JSON」布局）。新增平台使用 kebab-case ID（如 `github-copilot`）作为图标文件名。

### 改进

- 技能中心移到侧栏分割线下方，与软件平台、项目目录同组；图标与区块样式与后两者对齐；中文名称改为**技能中心**。
- 去掉软件平台、项目目录左侧的展开箭头，三项间距统一。
- 设置页软件平台去掉「打开平台文件夹」按钮。
- 检测标签改为「已检测到」/「未检测到」。
- 更新中英文 README 与截图，覆盖统一软件平台与配置目录结构。

## 0.70.0 - 2026-08-21

本版本把技能集合库放到中央技能库之上，不再把 Obsidian 仓库当成特殊来源，并把完整备份收窄为技能资源库和技能集合。

### 新功能

- 侧栏「技能集合」改为「技能集合库」，并排在技能资源库和中央技能库之间。
- 技能资源库、中央技能库、软件平台、项目目录和 Discover 标题栏中的目录路径可点击，在文件管理器中打开。
- 设置页已添加的项目目录列表可以像龙虾类、编程类一样折叠。

### 改进

- 本地和 WebDAV 完整备份包含技能资源库和技能集合，以及应用配置和已有平台安装关系。中央技能库不再导出或恢复。旧备份里的中央技能文件会在资源库尚不存在时迁入技能资源库。
- Vite 监听忽略 `release-assets` 和 `dist`，避免 Windows 打包安装包时把 `pnpm dev` 打死。
- 更新中英文 README 和截图，覆盖新的侧栏顺序、可点击路径、可折叠项目目录和备份范围。

### 修复

- 去掉 Obsidian 仓库检测、`/obsidian` 页面和侧栏 Obsidian 分组。Discover 不再把 `.obsidian` 当成特殊技能来源。
- 打开 Windows 目录前把路径转成反斜杠再交给资源管理器，避免点击 `C:/Users/...` 这类路径时打开「文档」。

## 0.60.0 - 2026-08-20

本版本把加入中央技能库改为指向资源库的符号链接，标出平台的独立/共享目录，并在按目录卸载后清理平台残留文件夹。

### 新功能

- 从技能资源库加入中央技能库时创建符号链接，不再复制一份。从中央库移除时只删除链接，资源库原文件保留。
- 与 `~/.agents/skills` 解析为同一路径的软件平台标为「共享目录」，其余为「独立目录」。设置页显示文字标签，侧栏在名称后、计数前用圆点区分。
- 设置页「平台与项目目录」增加总刷新，重新检测本机是否新增或卸载了平台，并刷新项目目录列表。
- 技能集合的「批量安装到」改为「安装」。弹窗可勾选软件平台、项目目录和中央技能库，默认全不选。

### 改进

- 仓库/来源列按链接实际指向的资源库或中央库显示。
- 从中央库按目录卸载后，清掉各平台和项目 skills 目录里留下的空文件夹和失效链接。
- `npx skills` 连不上 GitHub 时回退到 GitHub API 下载，并在 git clone 时使用已保存的 PAT。
- 更新中英文 README 和截图，覆盖中央库链接、设置页刷新、独立/共享目录标签和集合安装。

### 修复

- 资源库扫描不再把中央库指向资源库的合法符号链接当成旧数据删掉，避免加入中央库后条目消失。
- 按目录删除中央库套件时，会卸掉其中的符号链接子技能，并更新表格记录，不再只删文件夹、技能还留在平铺视图。
- 清理某个平台残留目录失败时，不再中断整次中央库目录删除。
- GitHub 不可达时，截断过长的导入错误提示。

## 0.50.2 - 2026-08-20

本版本让中央技能库目录与其他页面一样在页内展开表格，并去掉软件平台和项目目录的多选批量卸载。

### 改进

- 点击中央技能库目录后，同页展开该目录下的技能表，顶部提供「返回目录」，与技能资源库、软件平台和项目目录一致。
- 去掉软件平台和项目目录中的多选批量卸载。行内单个卸载和目录级卸载仍然保留。

## 0.50.1 - 2026-08-20

本版本让备份和恢复保持「中央技能库是技能资源库的子集」。

### 修复

- 完整备份会包含已加入中央库、但仍在资源库磁盘上的技能，恢复后两边都有。
- 如果备份里只有中央库副本，恢复时也会写入当前技能资源库。

## 0.50.0 - 2026-08-20

本版本让设置页路径更好操作，给技能集合补上刷新，并让备份和侧栏与磁盘上看到的内容一致。

### 新功能

- 设置页的配置文件路径、技能资源库目录、中央技能库目录增加「浏览」和「打开」。浏览选中目录后立即保存；粘贴或输入路径后按回车也会保存。
- 技能集合页标题旁增加刷新按钮，与技能资源库、中央技能库一致。

### 改进

- 本地和 WebDAV 备份都是完整备份：技能资源库、中央技能库、技能集合、自定义平台、来源缓存、已有平台安装关系，以及主题/语言等普通配置。导入写回本机当前的资源库和中央库目录，不会把另一台电脑上的路径带过来。
- 导出前先按磁盘同步技能资源库，使备份数量与页面上看到的一致。
- 更新中英文 README 和截图，覆盖设置页路径操作、技能集合刷新、侧栏平台和备份恢复。

### 修复

- 表格路径列收窄后会截断显示，不再盖住「技能数」。
- 软件平台里的 Obsidian 只展示官方 iCloud Obsidian 文档库。OneDrive 或本地库如果已添加为项目目录，只出现在项目目录中。
- 同一个文件夹已经是项目目录时，不再在软件平台里重复显示对应的 Obsidian 仓库。

## 0.40.0 - 2026-08-19

本版本让配置目录支持便携安装，把项目目录从软件平台列表中分开，并用系统保存对话框导出本地备份。

### 新功能

- 数据库、技能资源库和其他应用文件都放在 `.skillshub` 配置目录中。默认是 `~/.skillshub`；把 `.skillshub` 放到可执行文件同级即可便携使用，也可以在设置里指定其他路径。
- 项目目录可以填写显示名称，和软件平台一致。
- 底部状态栏显示导入、更新和安装的条目状态，包括未变化、本地跳过和无法更新。

### 改进

- 设置页顺序改为：配置文件路径、技能资源库、中央技能库，然后是软件平台和项目目录。
- 侧栏龙虾类、编程类下的平台清单与项目目录一样带缩进。
- 表格列双击按内容收窄，序号列更紧凑，窗口拉宽时多余空间分给各列，不再在右侧留白。
- WebDAV 备份时间按系统时区显示，新备份文件名也使用本地时间。
- 本地导出备份改为系统保存对话框，由后端直接写 ZIP，不再走网页下载。
- 更新中英文 README，覆盖配置目录、项目目录名称、侧栏层次和备份流程。

### 修复

- 已配置的项目目录不再出现在设置页「编程类」软件平台中。
- 导出备份时跳过磁盘上已缺失的技能目录，不再因此中断整次导出。
- 修复导出较大本地备份时 WebView 崩溃的问题。

## 0.30.0 - 2026-08-15

本版本把技能浏览统一成表格，精简技能集合页，并让共享 `.agents/skills` 目标按中央技能库处理，而不再安装失败。

### 新功能

- 技能资源库、中央技能库、软件平台和项目目录共用可调整列宽的表格，双击列边缘可自动适配内容宽度。
- 这些页面的「平铺 / 目录」切换移到搜索框右侧。
- 安装统计改为两行：直接安装（平台 / 项目）和共享可用，悬停可查看目标明细。
- 技能目录等于共享 `.agents/skills` 的平台或项目，安装时转换为加入中央技能库；已经在中央库中的目标显示为已通过中央库共享并禁用。
- 技能集合改为紧凑的平铺表格，保留新建、编辑、删除、批量安装和添加技能。

### 改进

- 中央技能库和平台/项目的加入、移除图标成套显示，提示文字不再带技能名称。
- 路径显示会去掉 Windows 的 `\\?\` 前缀。
- 资源库按目录安装时，如果本机无法创建符号链接，会自动回退为复制。
- 取消技能集合的导入/导出和目录视图，避免排版零碎和安装状态混乱。
- 更新中英文 README 和截图，覆盖当前表格、集合、平台和设置界面。

### 修复

- 修复从技能资源库目录视图安装到共享 `.agents/skills` 平台或项目时失败的问题。
- 技能加入中央库或安装到目标后，表格操作按钮顺序保持稳定。

## 0.20.1 - 2026-08-14

本补丁版本聚焦统一技能浏览工具栏，并保证技能资源库的标签和备注在扫描、导入、刷新后保持稳定。

### 改进

- 取消独立的正排和倒排按钮，名称、创建时间和修改时间按钮现在会在点击时切换排序方向。
- 技能资源库、中央技能库、软件平台和项目目录共用分组后的“排序”和“视图”工具栏。
- 提升排序和视图控件选中态的区分度，包括全部/目录视图切换。
- 设置页添加项目目录弹窗增加“浏览”按钮。
- 更新中英文 README 和截图，覆盖当前浏览、目录、平台、项目目录和设置界面。

### 修复

- 修复技能资源库在重新扫描、导入或刷新后丢失已有标签和备注的问题。
- 修复技能资源库目录视图中点击标签不能正确筛选的问题。
- 修复技能资源库按名称、创建时间、修改时间排序只对“全部”视图生效的问题。
- 导入技能完成后会自动刷新技能资源库。
- 修复 AI 备注生成成功后只写入少量字符、需要再次点击才完整显示的问题。

## 0.20.0 - 2026-08-13

本版本聚焦将技能导入、添加和更新统一到更可靠的本地资源库工作流，并改善更新可观测性。

### 新功能

- 导入技能改为基于隔离临时目录执行 `npx skills add owner/repo`，再复制完整技能文件夹到技能资源库。
- 导入技能支持可选技能名称；填写后只导入仓库中的指定技能，留空时导入仓库中可识别的全部技能。
- 新增本地“添加技能”流程，可复制已准备好的单个 skill 文件夹或合集文件夹到技能资源库，并与 npx 导入技能区分来源。
- 更新技能新增来源更新标记检查，远程来源未变化时跳过重新下载，变化时重新下载并覆盖本地副本。
- 底部状态栏显示导入、更新和安装任务的实时状态，并提供更新统计信息。
- 技能资源库、中央技能库、软件平台和项目目录支持目录级安装、加入中央库和卸载操作。

### 改进

- 技能资源库按 `owner/repo/skill` 存储 npx 导入技能，避免导入到不明确的 `local` 目录。
- Windows 下调用 `npx` 时隐藏后台命令行窗口，并改进 Node.js/npx 路径探测。
- 导入和添加技能弹窗将说明收纳到提示图标中，减少正文干扰。
- 本地技能文件夹输入框统一显示正常 Windows 单反斜杠路径。
- 单个技能和目录卡片使用一致但可区分的安装图标：数据库图标表示加入中央技能库，包安装图标表示安装到软件平台或项目目录。
- 技能详情页改为只读预览，并把时间、来源和存储路径合并为基本信息分区。
- 梳理本地打包脚本输出说明，明确 `dist/`、`src-tauri/target/` 和 `release-assets/` 的用途差异。
- 更新中英文 README 和界面截图，覆盖当前资源库、中央库、集合、设置、平台和项目目录界面。

### 修复

- 修复 npx 导入后技能详情页因路径和记录不一致而提示 not found 的问题。
- 修复从来源更新失败时只显示粗略错误的问题，状态栏和提示会包含失败技能与原因。
- 修复多技能仓库新增、删除或更新时资源库更新无法正确反映来源变化的问题。

## 0.16.0 - 2026-08-10

本版本聚焦将项目目录升级为一等安装目标，并让左侧栏和设置页的管理模型保持一致。

### 新功能

- 将已配置的项目目录作为安装目标，统一管理 `<项目>/.agents/skills` 下的技能。
- 技能资源库和中央技能库安装时，可以像选择软件平台一样选择项目目录。
- 技能资源库目录支持作为一个目录级操作加入中央技能库，或安装到软件平台、项目目录。
- 软件平台和项目目录的目录视图支持按目录卸载已安装技能。
- 中央技能库已有托管技能时，会自动同步到已启用的项目目录。
- 左侧栏的软件平台、龙虾类、编程类和项目目录都支持折叠。

### 改进

- 将旧的项目技能入口重命名为项目目录，并放在软件平台下方。
- 设置页调整为软件平台在上、项目目录在下。
- 技能资源库、中央技能库和平台状态在浏览器模式与 Tauri 模式下都能识别项目目录目标。
- 技能集合添加技能时改为从技能资源库选择，并让集合中的单个技能安装逻辑与资源库安装保持一致。
- 统一单个技能和目录卡片的安装图标语义，区分加入中央技能库和安装到目标。
- 根据新的左侧栏和项目目录安装模型更新中英文文档与截图。

### 修复

- 项目目录被禁用、不存在或删除配置时，清理对应的虚拟安装记录。
- 项目目录扫描到的技能数量会记录到对应的虚拟项目目标 id 下。
- 修复 `project:<id>` 这类虚拟目标路由编码后的项目目录页面识别。

## 0.15.0 - 2026-07-23

本版本聚焦中央技能同步、WebDAV 备份控制和发布前的界面一致性整理。

### 新功能

- 设置页 WebDAV 备份新增测试连接和删除选中远端备份。
- 中央技能库已有托管技能时，新检测到的本地平台会自动纳入中央技能同步。
- 设置页平台分组统计同时显示内置平台总数和当前电脑已检测到的平台数量。

### 改进

- WebDAV 备份操作整理为同一行右对齐按钮，并缩短按钮名称、补齐图标。
- 设置页备份、令牌、AI 提供商和检查更新等操作按钮统一靠右排布。
- AI 提供商和语言切换的选中态与左侧栏蓝色保持一致，并覆盖深色主题。
- 根据当前设置、平台、备份、导入和技能卡片界面更新中英文文档与截图。

### 修复

- 保持左侧栏平台列表与应接收中央技能同步的本地已检测平台一致。
- 修复中央同步后设置页已检测平台数量和左侧栏可见平台数量不一致的问题。
- 改进 WebDAV 远端列表、上传、导入、测试和删除操作的错误处理。

## 0.14.0 - 2026-07-23

本版本聚焦技能资源库工作流、来源更新可靠性、设置页整理和更统一的界面表现。

### 新功能

- 新增统一的技能资源库导入菜单，合并 GitHub 仓库导入和支持的 skills.sh 技能链接导入。
- GitHub-backed 技能导入到技能资源库时支持预览、选择、冲突处理和来源信息记录。
- 允许编辑、删除和恢复内置软件平台，并把修改持久化到本地配置。
- 设置页关于区域新增检查更新，可对比当前安装版本与 GitHub 最新发布版本。

### 改进

- 移除独立的技能市场页面和相关前端市场浏览界面。
- 技能资源库目录视图改为围绕 `作者/项目` 分组，而不是只显示作者列表。
- 技能卡片同时显示创建日期和更新日期，并按中英文界面使用对应语言标签。
- 只要技能具备可恢复的 GitHub 来源信息，即使缺少直接来源 URL，也显示单个技能的来源更新操作。
- 优化技能详情右侧栏，将备注、标签、来源信息、时间信息、存储路径、安装状态和技能集合整理为清晰分区。
- 设置页提示文案改为提示图标悬浮显示，并把目录保存按钮与对应路径输入框放在同一行，重新组织备份和 WebDAV 控件。
- 软件平台按龙虾类和编程类分组，每个分组内部保持响应式两列布局。
- 设置页区分已检测到 skills 目录和未检测到目录的内置平台；主侧边栏默认隐藏未检测到目录的内置平台。
- 主题和设置入口移动到左下角，主题中的“跟随系统”重命名为“系统”，移除顶部全局搜索框，并加宽展开后的侧边栏。
- 刷新中英文 README 截图，确保每份文档使用对应语言界面截图。
- 统一跨平台界面字体栈，降低字体割裂感。

### 修复

- 移除技能市场界面后保留 GitHub 导入能力。
- 修复技能资源库“从来源更新”错误更新中央技能库、导致资源库列表被清空的问题。
- 从已保存的 GitHub 仓库和路径信息恢复缺失的来源 URL，再执行来源更新。
- 来源更新覆盖本地 `SKILL.md` 前会校验下载内容，避免写入无效文件。
- 避免 GitHub 导入的技能因来源信息不完整而失去后续更新能力。
- 内置平台修改后可在应用重启后恢复。
- 减少技能详情右侧栏中的重复和未分组元数据。

## 0.13.0 - 2026-07-22

本版本聚焦内部发布准备和打包元数据。

## 0.12.0 - 2026-07-20

本版本聚焦软件平台管理、项目发现行为和分平台打包脚本。

### 新功能

- 将设置页中的扫描目录和自定义平台整合为更清晰的平台与项目目录管理区域。
- 将管理对象拆分为软件平台和项目目录，并支持查看内置软件平台。
- 新增 Windows、macOS、Linux 三个独立发布打包脚本，每个脚本只负责自己的宿主平台。

### 改进

- 本地不存在对应 skills 目录时，主界面不再显示该内置软件平台。
- 设置页默认折叠内置软件平台，让平台管理区域更紧凑。
- 优化设置页布局，把新增操作放到对应分区标题旁边。
- 更新中英文设置页截图以匹配当前界面。
- 优化 README 中的打包说明，改为平台专用命令和统一参数表。
- 停止追踪 `docs/` 下的本地规划文档，并从仓库移除生成型 `release-notes` 文件。

### 修复

- 未配置项目目录时，浏览器 fixture 中的项目技能不再出现在项目技能库。
- 项目技能库只显示来自已配置项目目录的结果；没有启用的项目目录时不再展示旧缓存项目技能。
- 修复 Homebrew Rust 覆盖 rustup toolchain 时 macOS universal 打包找不到目标标准库的问题。
- 修复 macOS 和 Linux 打包脚本中 `mktemp` 模板的跨平台兼容性。
